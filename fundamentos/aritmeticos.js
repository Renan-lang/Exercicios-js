const [a, e, i, o, u] = [15, 4, 8, 1, 5]

const soma  = a + e + i + o+ u
const subtracao = a - i
const multi = e * o
const divisao = i / u
const modulo = a % 2

console.log(soma, subtracao, multi, divisao, modulo)
